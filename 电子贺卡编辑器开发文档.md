# 电子贺卡编辑器开发文档

> **版本**：v1.0
> **日期**：2026-06-22
> **适用项目**：电子贺卡在线制作平台（H5编辑器）
> **参考基准**：贺卡工坊功能模块详细技术方案

---

## 目录

1. [项目概述与目标](#一项目概述与目标)
2. [系统架构与技术栈](#二系统架构与技术栈)
3. [核心数据模型](#三核心数据模型)
4. [API接口规范](#四api接口规范)
5. [功能模块清单](#五功能模块清单)
6. [业务流程](#六业务流程)
7. [关键实现细节](#七关键实现细节)
8. [开发环境配置](#八开发环境配置)
9. [测试策略](#九测试策略)
10. [部署流程](#十部署流程)
11. [代码规范](#十一代码规范)
12. [已知问题与优化建议](#十二已知问题与优化建议)

---

## 一、项目概述与目标

### 1.1 项目背景

本项目是一个电子贺卡在线制作平台，旨在为用户提供一个简单易用的H5贺卡编辑器。用户可以通过模板选择、元素添加、样式调整等操作，快速制作出精美的电子贺卡，并支持预览、导出和分享功能。

### 1.2 项目目标

#### 核心目标
- 提供直观的拖拽式编辑体验
- 支持多种元素类型（文本、图片、形状、音频等）
- 实现多页面贺卡制作能力
- 提供丰富的模板库供用户选择
- 支持贺卡预览、导出和分享

#### 技术目标
- 采用现代化前端技术栈（React 18 + TypeScript）
- 实现高性能的画布渲染
- 提供流畅的用户交互体验
- 保证代码可维护性和可扩展性

### 1.3 目标用户

- **普通用户**：需要制作电子贺卡的个人用户
- **设计师**：需要快速制作贺卡模板的专业用户
- **企业管理员**：管理模板和用户的后台管理员

---

## 二、系统架构与技术栈

### 2.1 技术选型

| 层级 | 技术方案 | 版本 | 说明 |
|------|---------|------|------|
| **前端框架** | React | 18.3.1 | 现代化组件化框架 |
| **开发语言** | TypeScript | 5.8.3 | 类型安全的JavaScript超集 |
| **构建工具** | Vite | 6.3.5 | 快速的开发构建工具 |
| **状态管理** | Zustand | 5.0.3 | 轻量级状态管理库 |
| **路由管理** | React Router | 7.3.0 | 声明式路由解决方案 |
| **样式方案** | Tailwind CSS | 3.4.17 | 原子化CSS框架 |
| **UI图标** | Lucide React | 0.511.0 | 现代化图标库 |
| **后端服务** | Supabase | 2.108.2 | BaaS平台，提供认证和数据库 |
| **PDF导出** | jsPDF | 4.2.1 | PDF生成库 |
| **截图导出** | html2canvas | 1.4.1 | HTML转Canvas库 |

### 2.2 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端 (浏览器)                          │
│                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ 编辑侧边栏 │  │ 画布区域  │  │ 属性面板  │  │ 工具栏    │       │
│  │ (左侧)   │  │ (中央)   │  │ (右侧)   │  │ (顶部)   │       │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘       │
│       │             │             │             │              │
│       └─────────────┴──────┬──────┴─────────────┘              │
│                            │                                   │
│                   ┌────────▼────────┐                          │
│                   │  Zustand Store   │                          │
│                   │ (状态管理中心)    │                          │
│                   └────────┬────────┘                          │
└────────────────────────────┼───────────────────────────────────┘
                             │ HTTP API
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        服务端                                  │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Supabase    │  │  Node.js API │  │  PostgreSQL  │          │
│  │  (认证服务)  │  │  (数据存储)   │  │   (数据库)   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 项目目录结构

```
e-card/
├── src/                              # 前端源码
│   ├── components/                   # 组件目录
│   │   ├── admin/                    # 管理后台组件
│   │   │   └── AdminLayout.tsx       # 后台布局
│   │   ├── common/                   # 公共组件
│   │   │   ├── Header.tsx            # 页头
│   │   │   └── Footer.tsx            # 页脚
│   │   ├── editor/                   # 编辑器组件
│   │   │   ├── Canvas.tsx            # 画布组件
│   │   │   ├── EditorSidebar.tsx     # 编辑侧边栏
│   │   │   ├── LayersPanel.tsx       # 图层面板
│   │   │   ├── MusicPanel.tsx        # 音乐面板
│   │   │   ├── PageSettingsPanel.tsx # 页面设置面板
│   │   │   ├── PagesPanel.tsx        # 页面管理面板
│   │   │   ├── PropertyPanel.tsx     # 属性面板
│   │   │   └── Toolbar.tsx           # 工具栏
│   │   └── templates/                # 模板组件
│   │       ├── FilterPanel.tsx       # 筛选面板
│   │       └── TemplateCard.tsx      # 模板卡片
│   ├── data/                         # 模拟数据
│   │   └── mockTemplates.ts          # 模板mock数据
│   ├── hooks/                        # 自定义Hooks
│   │   ├── useAuth.ts                # 认证Hook
│   │   ├── useKeyboardShortcuts.ts   # 快捷键Hook
│   │   └── useTheme.ts               # 主题Hook
│   ├── lib/                          # 工具库
│   │   └── utils.ts                  # 工具函数
│   ├── pages/                        # 页面组件
│   │   ├── admin/                    # 管理后台页面
│   │   │   ├── AdminLoginPage.tsx    # 后台登录页
│   │   │   ├── CardManagementPage.tsx # 贺卡管理
│   │   │   ├── DashboardPage.tsx     # 仪表盘
│   │   │   ├── TemplateEditorPage.tsx # 模板编辑器
│   │   │   ├── TemplateManagementPage.tsx # 模板管理
│   │   │   └── UserManagementPage.tsx # 用户管理
│   │   ├── EditorPage.tsx            # 编辑器主页面
│   │   ├── ExportPage.tsx            # 导出页面
│   │   ├── ForgotPasswordPage.tsx    # 忘记密码页
│   │   ├── HomePage.tsx              # 首页
│   │   ├── LoginPage.tsx             # 登录页
│   │   ├── PreviewPage.tsx           # 预览页面
│   │   ├── RegisterPage.tsx          # 注册页
│   │   ├── TemplateLibrary.tsx       # 模板库页面
│   │   ├── UserProfile.tsx           # 用户资料页
│   │   └── VerificationSentPage.tsx  # 验证发送页
│   ├── store/                        # 状态管理
│   │   └── index.ts                  # Zustand Store定义
│   ├── types/                        # TypeScript类型定义
│   │   └── index.ts                  # 类型声明
│   ├── utils/                        # 工具函数
│   │   ├── export.ts                 # 导出工具
│   │   └── supabase.ts               # Supabase客户端封装
│   ├── App.tsx                       # 应用入口组件
│   ├── index.css                     # 全局样式
│   └── main.tsx                      # 主入口文件
├── server/                           # 后端服务
│   ├── scripts/                      # 数据库脚本
│   ├── server.js                     # Express服务器
│   └── package.json                  # 后端依赖
├── db/                               # 数据库相关
│   └── init/                         # 数据库初始化脚本
├── supabase/                         # Supabase配置
│   └── functions/                    # Supabase边缘函数
└── public/                           # 静态资源
```

---

## 三、核心数据模型

### 3.1 CardElement（贺卡元素）

```typescript
interface CardElement {
  id: string;
  type: 'text' | 'image' | 'shape' | 'audio' | 'video' | 'icon' | 'group';
  content: string;
  position: { x: number; y: number };      // 百分比位置
  size?: { width: number; height: number }; // 百分比尺寸
  rotation?: number;                        // 旋转角度
  zIndex?: number;                          // 层级
  visible?: boolean;                        // 可见性
  locked?: boolean;                         // 锁定状态
  children?: string[];                      // 子元素ID（组元素）
  childElements?: CardElement[];            // 子元素（组元素）
  style: {
    fontSize?: number;
    fontFamily?: string;
    color?: string;
    opacity?: number;
    fontWeight?: string;
    textAlign?: string;
    backgroundColor?: string;
    borderRadius?: number;
    borderWidth?: number;
    borderColor?: string;
    textShadow?: string;
    boxShadow?: string;
    gradient?: {
      type: 'linear' | 'radial';
      colors: string[];
      angle?: number;
      stops?: number[];
    };
    animation?: string;
    animationDuration?: number;
    animationDelay?: number;
  };
}
```

### 3.2 CardPage（贺卡页面）

```typescript
interface CardPage {
  id: string;
  pageNumber: number;
  backgroundUrl?: string;           // 页面背景图
  backgroundColor?: string;         // 页面背景色
  elements: CardElement[];          // 页面元素
  audioUrl?: string;                // 页面专属音频
  audioLoop?: boolean;              // 音频循环
  audioAutoplay?: boolean;          // 音频自动播放
  transition?: 'none' | 'fade' | 'slide' | 'zoom' | 'flip';
  transitionDuration?: number;      // 动画时长
}
```

### 3.3 Card（贺卡项目）

```typescript
interface Card {
  id: string;
  user_id: string;
  template_id: string;
  title: string;
  pages: CardPage[];                // 多页贺卡
  background_music_url?: string;    // 全局背景音乐
  backgroundMusicLoop?: boolean;
  cover?: string;                   // 贺卡封面
  description?: string;             // 贺卡描述
  created_at: string;
  updated_at: string;
}
```

### 3.4 Template（模板）

```typescript
interface Template {
  id: string;
  name: string;
  category: string;
  occasion: string;
  style: string;
  thumbnail_url: string;
  background_url: string;
  default_elements: CardElement[];
  pages?: CardPage[];               // 多页模板
  backgroundMusicUrl?: string;      // 模板背景音乐
  created_at: string;
}
```

### 3.5 User（用户）

```typescript
interface User {
  id: string;
  email?: string;
  phone?: string;
  name?: string;
  created_at: string;
  is_admin?: boolean;
  is_email_verified?: boolean;
  is_phone_verified?: boolean;
  is_disabled?: boolean;
  last_login_at?: string;
}
```

---

## 四、API接口规范

### 4.1 接口总览

| 模块 | API路径 | HTTP方法 | 认证要求 | 描述 |
|------|---------|----------|---------|------|
| **认证** | `/api/auth/signin` | POST | 否 | 用户登录 |
| **认证** | `/api/auth/signup` | POST | 否 | 用户注册 |
| **认证** | `/api/auth/signout` | POST | 是 | 用户登出 |
| **认证** | `/api/auth/me` | GET | 是 | 获取当前用户 |
| **认证** | `/api/auth/sync-user` | POST | 是 | 同步用户到本地数据库 |
| **贺卡** | `/api/cards` | GET | 是 | 获取用户贺卡列表 |
| **贺卡** | `/api/cards` | POST | 是 | 创建新贺卡 |
| **贺卡** | `/api/cards/:id` | GET | 是 | 获取贺卡详情 |
| **贺卡** | `/api/cards/:id` | PUT | 是 | 更新贺卡 |
| **贺卡** | `/api/cards/:id` | DELETE | 是 | 删除贺卡 |
| **模板** | `/api/templates` | GET | 否 | 获取模板列表 |
| **模板** | `/api/templates/:id` | GET | 否 | 获取模板详情 |
| **模板** | `/api/templates` | POST | 是(管理员) | 创建模板 |
| **模板** | `/api/templates/:id` | PUT | 是(管理员) | 更新模板 |
| **模板** | `/api/templates/:id` | DELETE | 是(管理员) | 删除模板 |
| **收藏** | `/api/favorites` | GET | 是 | 获取用户收藏 |
| **收藏** | `/api/favorites` | POST | 是 | 添加收藏 |
| **收藏** | `/api/favorites` | DELETE | 是 | 删除收藏 |
| **管理** | `/api/admin/users` | GET | 是(管理员) | 获取用户列表 |
| **管理** | `/api/admin/users/:id` | PUT | 是(管理员) | 更新用户 |
| **管理** | `/api/admin/users/:id` | DELETE | 是(管理员) | 删除用户 |
| **管理** | `/api/admin/cards` | GET | 是(管理员) | 获取所有贺卡 |
| **管理** | `/api/admin/cards/:id` | DELETE | 是(管理员) | 删除贺卡 |
| **管理** | `/api/admin/stats` | GET | 是(管理员) | 获取统计数据 |

### 4.2 认证接口

**POST /api/auth/signin**

请求体：
```json
{
  "email": "string",      // 用户邮箱
  "password": "string"    // 用户密码
}
```

成功响应（200）：
```json
{
  "success": true,
  "data": {
    "id": "string",
    "email": "string",
    "name": "string",
    "is_admin": false,
    "created_at": "string"
  },
  "token": "string"
}
```

### 4.3 贺卡接口

**GET /api/cards**

请求头：`Authorization: Bearer <token>`

成功响应（200）：
```json
{
  "success": true,
  "data": [{ "id": "string", "title": "string", "pages": [], "created_at": "string" }],
  "pagination": { "page": 1, "limit": 20, "total": 100, "total_pages": 5 }
}
```

### 4.4 模板接口

**GET /api/templates**

查询参数：`category`, `occasion`, `style`, `page`, `limit`

成功响应（200）：
```json
{
  "success": true,
  "data": [{ "id": "string", "name": "string", "category": "string" }],
  "pagination": { "page": 1, "limit": 20, "total": 50 }
}
```

---

## 五、功能模块清单

### 5.1 编辑器核心功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 文本元素 | 添加文本、编辑内容、样式调整 | [Toolbar.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Toolbar.tsx) | ✅ |
| 图片元素 | 上传图片（自动压缩）、拖拽调整 | [Toolbar.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Toolbar.tsx) | ✅ |
| 形状元素 | 矩形、圆形、三角形、线条、箭头、星形 | [Toolbar.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Toolbar.tsx) | ✅ |
| 表情/图标 | 添加表情符号 | [Toolbar.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Toolbar.tsx) | ✅ |
| 元素拖拽 | 拖拽移动元素位置 | [Canvas.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Canvas.tsx) | ✅ |
| 元素缩放 | 拖拽调整元素大小 | [Canvas.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Canvas.tsx) | ✅ |
| 元素旋转 | 旋转元素 | [PropertyPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PropertyPanel.tsx) | ✅ |
| 对齐工具 | 左/中/右/上/中/下对齐 | [Toolbar.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Toolbar.tsx) | ✅ |
| 撤销/重做 | 最多50步历史记录 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ✅ |

### 5.2 页面管理功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 添加页面 | 创建新页面 | [PagesPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PagesPanel.tsx) | ✅ |
| 删除页面 | 删除指定页面 | [PagesPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PagesPanel.tsx) | ✅ |
| 复制页面 | 复制当前页面 | [PagesPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PagesPanel.tsx) | ✅ |
| 页面排序 | 拖拽调整顺序 | [PagesPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PagesPanel.tsx) | ✅ |
| 页面切换 | 点击缩略图切换 | [PagesPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PagesPanel.tsx) | ✅ |
| 页面动画 | 淡入淡出、滑动、缩放、翻转 | [PageSettingsPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PageSettingsPanel.tsx) | ✅ |

### 5.3 属性面板功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 文本属性 | 字体大小、颜色、粗细、对齐 | [PropertyPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PropertyPanel.tsx) | ✅ |
| 图片属性 | 透明度、圆角、边框 | [PropertyPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PropertyPanel.tsx) | ✅ |
| 形状属性 | 填充颜色、边框、圆角 | [PropertyPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PropertyPanel.tsx) | ✅ |
| 位置尺寸 | X/Y坐标、宽度/高度 | [PropertyPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PropertyPanel.tsx) | ✅ |
| 旋转角度 | -180° ~ 180° | [PropertyPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PropertyPanel.tsx) | ✅ |

### 5.4 背景与音乐功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 背景色设置 | 页面背景颜色 | [PageSettingsPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PageSettingsPanel.tsx) | ✅ |
| 背景图设置 | 页面背景图片 | [PageSettingsPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PageSettingsPanel.tsx) | ✅ |
| 全局音乐 | 贺卡背景音乐 | [MusicPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/MusicPanel.tsx) | ✅ |
| 页面音频 | 页面专属音频 | [PageSettingsPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/PageSettingsPanel.tsx) | ✅ |

### 5.5 图层层级功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 置顶 | 元素置于最上层 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ✅ |
| 置底 | 元素置于最下层 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ✅ |
| 上移一层 | 元素上移一层 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ✅ |
| 下移一层 | 元素下移一层 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ✅ |
| 可见性切换 | 显示/隐藏元素 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ✅ |
| 锁定切换 | 锁定/解锁元素 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ✅ |

### 5.6 组合功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 组合元素 | 将多个元素组合 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ⚠️ |
| 拆分组合 | 拆分组合元素 | [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) | ⚠️ |

### 5.7 模板系统功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 模板浏览 | 模板库展示 | [TemplateLibrary.tsx](file:///E:/workspaceForTrae/e-card/src/pages/TemplateLibrary.tsx) | ✅ |
| 分类筛选 | 按类别/场合/风格筛选 | [FilterPanel.tsx](file:///E:/workspaceForTrae/e-card/src/components/templates/FilterPanel.tsx) | ✅ |
| 模板创建 | 从模板创建贺卡 | [EditorPage.tsx](file:///E:/workspaceForTrae/e-card/src/pages/EditorPage.tsx) | ✅ |

### 5.8 预览与导出功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 贺卡预览 | 翻页预览效果 | [PreviewPage.tsx](file:///E:/workspaceForTrae/e-card/src/pages/PreviewPage.tsx) | ✅ |
| PDF导出 | 导出为PDF | [ExportPage.tsx](file:///E:/workspaceForTrae/e-card/src/pages/ExportPage.tsx) | ✅ |
| 图片导出 | 导出为图片 | [ExportPage.tsx](file:///E:/workspaceForTrae/e-card/src/pages/ExportPage.tsx) | ✅ |

### 5.9 用户认证功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 用户注册 | 邮箱注册 | [useAuth.ts](file:///E:/workspaceForTrae/e-card/src/hooks/useAuth.ts) | ✅ |
| 用户登录 | 邮箱密码登录 | [useAuth.ts](file:///E:/workspaceForTrae/e-card/src/hooks/useAuth.ts) | ✅ |
| 邮箱验证 | 邮箱地址验证 | [supabase.ts](file:///E:/workspaceForTrae/e-card/src/utils/supabase.ts) | ✅ |
| 密码重置 | 忘记密码 | [useAuth.ts](file:///E:/workspaceForTrae/e-card/src/hooks/useAuth.ts) | ✅ |

### 5.10 管理员后台功能

| 功能 | 描述 | 实现位置 | 完成状态 |
|------|------|---------|---------|
| 用户管理 | 查看/编辑/删除用户 | [UserManagementPage.tsx](file:///E:/workspaceForTrae/e-card/src/pages/admin/UserManagementPage.tsx) | ✅ |
| 模板管理 | 查看/编辑/删除模板 | [TemplateManagementPage.tsx](file:///E:/workspaceForTrae/e-card/src/pages/admin/TemplateManagementPage.tsx) | ✅ |
| 贺卡管理 | 查看/删除贺卡 | [CardManagementPage.tsx](file:///E:/workspaceForTrae/e-card/src/pages/admin/CardManagementPage.tsx) | ✅ |
| 统计数据 | 查看平台统计 | [DashboardPage.tsx](file:///E:/workspaceForTrae/e-card/src/pages/admin/DashboardPage.tsx) | ✅ |

---

## 六、业务流程

### 6.1 用户注册流程

```
用户访问注册页 → 输入邮箱密码 → 调用Supabase注册 → 同步用户到本地数据库 → 发送验证邮件 → 跳转验证提示页
```

### 6.2 用户登录流程

```
用户访问登录页 → 输入邮箱密码 → 调用Supabase登录 → 获取本地用户信息（含管理员状态） → 验证用户是否被禁用 → 登录成功
```

### 6.3 贺卡编辑流程

```
选择模板 → 加载模板数据 → 创建编辑器状态 → 用户编辑操作（添加/修改/删除元素） → 自动保存历史记录 → 保存到数据库
```

### 6.4 贺卡预览流程

```
进入预览页 → 加载当前贺卡数据 → 渲染画布 → 播放背景音乐 → 用户翻页浏览 → 支持音频控制
```

### 6.5 贺卡导出流程

```
进入导出页 → 选择导出格式（PDF/图片） → 使用html2canvas截图 → 使用jsPDF生成PDF → 下载文件
```

---

## 七、关键实现细节

### 7.1 状态管理设计

项目采用Zustand作为状态管理库，实现了三个核心Store：

**EditorStore** - 编辑器状态管理：
- 管理当前贺卡数据（页面、元素、背景音乐）
- 管理选中元素和预览模式
- 实现撤销/重做系统（基于命令模式，最多50步历史记录）

**TemplatesStore** - 模板状态管理：
- 管理模板列表和分类信息
- 管理筛选条件（类别、场合、风格）

**UserStore** - 用户状态管理：
- 管理用户信息和认证状态
- 管理用户设计作品和收藏

### 7.2 画布渲染系统

画布采用原生HTML + CSS实现，而非Canvas API：

**设计优势**：
- **百分比坐标系统**：所有位置和尺寸使用百分比，实现响应式布局
- **性能优化**：避免Canvas重绘开销，利用浏览器原生渲染
- **易于调试**：可直接使用浏览器开发者工具检查元素

### 7.3 拖拽实现

元素拖拽采用原生鼠标事件实现，核心逻辑在 [Canvas.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Canvas.tsx) 中：

```typescript
const handleMouseDown = (e: React.MouseEvent, element: CardElement) => {
  selectElement(element.id);
  setDragging(element.id);
  const rect = (e.target as HTMLElement).getBoundingClientRect();
  setDragOffset({
    x: e.clientX - rect.left,
    y: e.clientY - rect.top,
  });
};
```

### 7.4 历史记录实现

基于命令模式实现撤销/重做，核心逻辑在 [store/index.ts](file:///E:/workspaceForTrae/e-card/src/store/index.ts) 中：

```typescript
interface HistoryEntry {
  type: 'addElement' | 'updateElement' | 'deleteElement' | ...;
  prevState: EditorState['currentCard'];
  nextState: EditorState['currentCard'];
}
```

### 7.5 图片压缩实现

上传图片时自动压缩，核心逻辑在 [Toolbar.tsx](file:///E:/workspaceForTrae/e-card/src/components/editor/Toolbar.tsx) 中：

```typescript
const compressImage = (file: File, maxWidth: number, maxHeight: number, quality: number): Promise<string> => {
  // 使用Canvas进行图片压缩
};
```

### 7.6 认证集成

采用Supabase + 本地API双层认证：
- **Supabase**：处理OAuth认证、邮箱验证、密码重置
- **本地API**：存储用户扩展信息（管理员状态、禁用状态等）

---

## 八、开发环境配置

### 8.1 环境要求

| 依赖 | 版本 | 说明 |
|------|------|------|
| Node.js | >= 18.x | JavaScript运行时 |
| npm | >= 9.x | 包管理器 |
| PostgreSQL | >= 14.x | 数据库（可选） |
| Git | >= 2.x | 版本控制 |

### 8.2 前端环境配置

```bash
# 安装依赖
npm install

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件配置 Supabase 和 API 地址

# 启动开发服务器
npm run dev
```

### 8.3 后端环境配置

```bash
cd server
npm install

# 创建数据库
createdb -U postgres ecard
psql -U postgres -d ecard -f ../db/init/01-schema.sql

# 启动后端服务
npm run dev
```

### 8.4 Mock模式

项目支持Mock模式，无需后端服务即可开发：

```bash
# 在 .env 中设置
VITE_USE_MOCK=true
```

---

## 九、测试策略

### 9.1 测试分类

| 测试类型 | 描述 | 工具 |
|---------|------|------|
| 单元测试 | 测试单个函数或组件 | Vitest |
| 集成测试 | 测试模块协同工作 | Vitest + React Testing Library |
| 性能测试 | 测试性能指标 | Lighthouse |

### 9.2 测试命令

```bash
# 运行单元测试
npm test

# 运行测试并生成覆盖率报告
npm run test:coverage

# 性能测试
npx lighthouse http://localhost:5173
```

### 9.3 测试文件结构

```
src/
└── tests/
    ├── setup.ts                     # 测试环境配置
    ├── store.test.ts                # Store单元测试
    ├── editorStoreEdgeCases.test.ts # 编辑器边界测试
    ├── templatesStore.test.ts       # 模板Store测试
    └── userStore.test.ts            # 用户Store测试
```

---

## 十、部署流程

### 10.1 前端部署

```bash
# 构建生产版本
npm run build

# Nginx配置示例
server {
  listen 443 ssl;
  server_name your-domain.com;
  root /var/www/ecard/dist;
  try_files $uri $uri/ /index.html;
  
  location /api/ {
    proxy_pass http://localhost:3000/api/;
  }
}
```

### 10.2 后端部署

```bash
# 使用PM2管理进程
npm install -g pm2
pm2 start server.js --name ecard-api
pm2 startup
pm2 save
```

---

## 十一、代码规范

### 11.1 TypeScript规范

| 类型 | 规则 | 示例 |
|------|------|------|
| 变量/函数 | 小驼峰 | `userName`, `getUser()` |
| 类/接口 | 大驼峰 | `CardElement`, `UserService` |
| 常量 | 全大写下划线 | `MAX_HISTORY` |
| 文件 | 大驼峰 | `EditorPage.tsx` |

### 11.2 React规范

- 组件名使用大驼峰
- 自定义Hook以`use`开头
- Props使用interface定义
- 保持组件职责单一

### 11.3 样式规范

- 使用Tailwind CSS工具类
- 避免内联样式（动态样式除外）
- 组件样式使用scoped样式

---

## 十二、已知问题与优化建议

### 12.1 功能限制

1. **图片存储**：当前使用Base64存储图片，大图片会增加数据库负担
   - **建议**：集成OSS对象存储服务

2. **撤销步数限制**：最多支持50步撤销
   - **建议**：可根据需要调整MAX_HISTORY值

3. **移动端适配**：编辑器主要针对桌面端设计
   - **建议**：开发响应式布局或移动端专用版本

### 12.2 性能限制

1. **大量元素性能**：超过50个元素时可能出现卡顿
   - **建议**：实现虚拟滚动或Canvas渲染优化

2. **大文件上传**：超过10MB的图片可能导致保存失败
   - **建议**：增强图片压缩或使用分块上传

### 12.3 兼容性限制

1. **不支持IE浏览器**：使用了ES6+语法和现代浏览器API
   - **建议**：明确浏览器支持范围

---

## 附录：功能完成度总览

| 优先级 | 功能模块 | 完成状态 | 完成度 |
|--------|---------|---------|--------|
| **P0** | 形状元素支持 | ✅ 已完成 | 80% |
| **P0** | 背景设置 | ✅ 已完成 | 90% |
| **P0** | 完整属性面板 | ✅ 已完成 | 70% |
| **P0** | 图层层级操作 | ✅ 已完成 | 50% |
| **P0** | 元素通用操作 | ✅ 已完成 | 85% |
| **P0** | 元素变换 | ✅ 已完成 | 90% |
| **P1** | 组合与拆分 | ⚠️ 部分完成 | 30% |
| **P1** | 快捷键体系 | ⚠️ 部分完成 | 40% |
| **P1** | 视频元素 | ❌ 未完成 | 0% |
| **P1** | 元素动画/特效 | ⚠️ 部分完成 | 30% |
| **P1** | 页面背景音乐全局管理 | ✅ 已完成 | 95% |
| **P2** | 模板中心完善 | ⚠️ 部分完成 | 40% |
| **P2** | 预览与设置 | ✅ 已完成 | 80% |
| **P3** | 多格式发布导出 | ⚠